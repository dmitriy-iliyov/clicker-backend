spring.application.name=clicker-api

# ssl
server.port=8443
server.ssl.key-store=classpath:/ssl/springboot_keystore.p12
server.ssl.key-store-password=password
server.ssl.key-password=password

# postgresql
spring.datasource.url=jdbc:postgresql://localhost:5432/database
spring.datasource.username=admin
spring.datasource.password=root
spring.datasource.driver-class-name=org.postgresql.Driver

# redis
spring.data.redis.host=localhost
spring.data.redis.port=6379
spring.data.redis.password=redisroot

# hibernate
spring.jpa.hibernate.ddl-auto=create

# sql logs
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# auth jwt
clicker.api.auth.jwt.secret=ekj130PdaOf4qpmd-pwqpd-p[23dnXMRQ

# wallet jwt
clicker.api.wallet.jwt.secret=1Jri54Woe-04rqWEfba-3pjf0lrnfaoas

# company mail
spring.mail.username=
spring.mail.password=

# ttl confirm token and cached unconf user
clicker.api.confirm.ttl.secs=86400

# ttl pass recovery token
clicker.api.password.recovery.ttl.secs=300

clicker.api.clicker.cached.probability.ttl.secs = 43200